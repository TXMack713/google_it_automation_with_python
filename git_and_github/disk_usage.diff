--- disk_usage_original.py	2023-10-05 10:17:57.358776517 -0500
+++ disk_usage_fixed.py	2023-10-05 10:21:55.593783210 -0500
@@ -3,22 +3,25 @@
 # Google IT Automation with Python - Git and GitHub, Practical Application of diff and patch
 
 import shutil
+import sys
 
 def check_disk_usage(disk, min_absolute, min_percent):
     """Returns True if there is enough free disk space, false otherwise."""
     du = shutil.disk_usage(disk)
     # Calculate the percentage of free space
     percent_free = 100 * du.free / du.total
+    print("Disk percentage free: " + str(percent_free))
     # Calculate how many free gigabytes
     gigabytes_free = du.free / 2**30
+    print("Free gigs: " + str(gigabytes_free))
     if percent_free < min_percent or gigabytes_free < min_absolute:
         return False
     return True
 
 # Check for at least 2 GB and 10% free
-if not check_disk_usage("/", 2*2**30, 10):
+if not check_disk_usage("/", 2, 10):
     print("ERROR: Not enough disk space")
-    return 1
+    sys.exit(1)
 
 print("Everything ok")
-return 0
\ No newline at end of file
+sys.exit(0)
\ No newline at end of file
